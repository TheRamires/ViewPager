<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentStart">
    <fragment
        android:id="@+id/fragmentStart"
        android:name="ram.ramires.viewpagerfrom_fragment.FragmentStart"
        android:label="fragment_start"
        tools:layout="@layout/fragment_start" >
        <action
            android:id="@+id/action_fragmentStart_to_fragmentConteiner"
            app:destination="@id/fragmentConteiner" />
    </fragment>
    <fragment
        android:id="@+id/fragmentConteiner"
        android:name="ram.ramires.viewpagerfrom_fragment.FragmentConteiner"
        android:label="fragment_conteiner"
        tools:layout="@layout/fragment_conteiner" />
</navigation>